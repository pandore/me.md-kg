# AI Provider (for fact extraction + interviews)
ANTHROPIC_API_KEY=sk-ant-...

# Voyage AI (for entity/relation embeddings)
VOYAGE_API_KEY=pa-...

# SurrealDB (embedded mode uses file path, no auth needed)
SURREAL_DB_PATH=~/.memd/kg.db

# OpenClaw workspace (for seeding from existing data)
OPENCLAW_WORKSPACE=~/.openclaw/workspaces/main
