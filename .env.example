# AI Provider (for fact extraction + interviews)
ANTHROPIC_API_KEY=sk-ant-...

# OpenAI (for semantic search embeddings — text-embedding-3-small)
OPENAI_API_KEY=sk-...

# Database path (SQLite file)
MEMD_DB_PATH=~/.memd/kg.db

# OpenClaw workspace (for seeding from existing data)
OPENCLAW_WORKSPACE=~/.openclaw/workspace
